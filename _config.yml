# Site settings
title: microft
tag_text: microft
description: microft
url: "https//microft.github.io"

# baseurl: "/blog" # the subpath of your site, e.g. /blog
# url: "https://example.com" # the base hostname & protocol for your site, e.g. http://example.com

plugins:
  - jekyll-seo-tag

# Build settings
markdown: kramdown
exclude: ["README.md"]
include:
  - assets
sass:
  style: compressed
